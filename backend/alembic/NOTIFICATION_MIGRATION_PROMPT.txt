Add notifications table with indexes on user_id, type, scheduled_date. sent default False.

Columns:
- id: integer primary key
- user_id: integer FK users.id (indexed)
- type: varchar(50) (indexed)
- title: varchar(200)
- message: text
- scheduled_date: timestamp with timezone nullable (indexed)
- sent: boolean default False
- created_at: timestamp with timezone default now()

Autogenerate hint: create indexes on (`user_id`), (`type`), and (`scheduled_date`) and set `sent` column default to False.